import { StyleSheet } from 'react-native';
import { commonColor } from '../../../shared/styles/common';

export const styles = StyleSheet.create({
  checkBoxText: {
    textDecorationLine: 'none',
    color: commonColor.white,
  },
});
